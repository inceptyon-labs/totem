# totem-prime

A Claude Code plugin that wires Totem into your agent automatically. Adds SessionStart and PreCompact hooks that run `totem prime`, so the agent always has up-to-date context about your project's totems (issues/tasks) without manual settings edits.

See the main [Totem README](../../../README.md) for setup instructions.
